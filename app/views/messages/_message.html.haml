.message
  .message--top
    .message--top__user
      = message.user.name
    .message--top__time_stamp
      = message.created_at
  .message__text
    - if message.body.present?
      = message.body
  = image_tag message.image.url, class: 'message__image' if message.image.present?